spring:
  datasource:
    url: jdbc:postgresql://postgres.database.svc.cluster.local:5432/taskmanager
    username: postgres
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080